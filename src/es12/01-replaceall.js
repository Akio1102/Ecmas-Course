//El método replace recibe como parametro un string el cual vamos a modificar el problema es que este solo modifica el 1 elemento que le pasemos

//El método replaceAll retorna un nuevo string, reemplazando todos los elementos por otro.

const string = "JS is a language of programming";
const replacedString = string.replace("JS", "TypeScript");
console.log(replacedString);
